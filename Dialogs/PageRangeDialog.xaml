<Window x:Class="HoloPDFCreator.Dialogs.PageRangeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Page Range"
        Width="300" Height="180"
        Background="{StaticResource BgGradient}"
        Foreground="#CDD6F4"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="FontSize"   Value="13"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background"      Value="{StaticResource GlassSurface}"/>
            <Setter Property="Foreground"      Value="#ECEFFF"/>
            <Setter Property="BorderBrush"     Value="{StaticResource GlassBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"         Value="6,4"/>
            <Setter Property="FontSize"        Value="13"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20,16,20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Specify page range for OCR:"
                   Foreground="#A6ADC8" FontSize="12"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="From page:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox   x:Name="TxtFrom" Grid.Column="1" Text="1"
                       VerticalContentAlignment="Center"
                       PreviewTextInput="NumericOnly_PreviewTextInput"/>
            <TextBlock Grid.Column="2" Text="  to  " VerticalAlignment="Center"/>
            <TextBox   x:Name="TxtTo" Grid.Column="3" Text="1"
                       VerticalContentAlignment="Center"
                       PreviewTextInput="NumericOnly_PreviewTextInput"/>
            <TextBlock x:Name="TxtTotalHint" Grid.Column="4"
                       Foreground="#585B70" FontSize="11"
                       VerticalAlignment="Center" Margin="6,0,0,0"/>
        </Grid>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="Cancel" MinWidth="80"
                    Style="{StaticResource SecondaryButton}" Click="Cancel_Click"/>
            <Button Grid.Column="3" Content="OK" MinWidth="80"
                    Style="{StaticResource PrimaryButton}" Click="Ok_Click"/>
        </Grid>
    </Grid>
</Window>
